.DS_Store
node_modules
package-lock.json
/dist
src/components.d.ts
src/auto-imports.d.ts


# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Backtest queue output artifacts (regenerated on each run)
backtest_queue/

# Freqtrade runtime data — contains secrets (api_server.jwt_secret_key / password)
# and live trade DB. Must never enter the repo. Matches both a directory and the
# local symlink that points at the freqtrade repo's user_data/ tree.
user_data
.omc/
